{{- if and .Values.alloy.enabled .Values.alloy.ingress.enabled }}
{{- $ingress := .Values.alloy.ingress }}
{{- $host := required "alloy.ingress.host is required" $ingress.host }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alloy-faro-ingress
  namespace: {{ include "beacon.namespace" . }}
  labels:
    {{- include "beacon.labels" . | nindent 4 }}
    {{- include "beacon.componentLabels" "alloy" | nindent 4 }}
  {{- with $ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ $ingress.className }}
  {{- if $ingress.tls }}
  tls:
    - hosts:
        - {{ $host | quote }}
      secretName: {{ $ingress.tls.secretName }}
  {{- end }}
  rules:
  - host: {{ $host | quote }}
    http:
      paths:
      - path: {{ $ingress.path }}
        pathType: ImplementationSpecific
        backend:
          service:
            name: alloy
            port:
              number: {{ .Values.alloy.service.faroPort }}
{{- end }}
